Demo中未考虑大数据包的接收问题，需要进行改造。
直接使用Demo接收时可能会有数据包接收不完整的问题。

IFD-X的Frm输出数据量比较大，建议在Android中使用一个独立的接收线程，每间隔1ms读取一次接收缓存，若超过20ms未收到新的数据则认为一包数据接收完毕。
数据正确性的判断：
向模块发送$SETP=17,4，模块会返回一包以MLX_EEM为前缀的数据包，[7]*256+[8]为第0个字，[9]*256+[10]为第1个字，以此类推，第12个字应固定为0x1901。
